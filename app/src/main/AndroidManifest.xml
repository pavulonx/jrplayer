<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.rozen.swim.jrplayer">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="pl.rozen.swim.jrplayer.activities.MainActivity"
            android:label="@string/main_activity_label"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="pl.rozen.swim.jrplayer.activities.AboutActivity"
            android:label="@string/about_activity_label"
            android:parentActivityName="pl.rozen.swim.jrplayer.activities.MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="pl.rozen.swim.jrplayer.activities.AudioDetailActivity"
            android:label="@string/audio_activity_label"
            android:parentActivityName="pl.rozen.swim.jrplayer.activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--<service android:name="pl.rozen.swim.jrplayer.services.MusicService" />-->

    </application>

</manifest>